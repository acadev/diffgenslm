# Core
torch>=2.1.0
numpy>=1.24
h5py>=3.8
sentencepiece>=0.1.99
tokenizers>=0.15
pyyaml>=6.0
tqdm>=4.65

# Genomics
biopython>=1.81

# Logging
wandb>=0.15

# MPI (install separately based on cluster)
# mpi4py>=3.1

# GenSLM-2 tokenizer (install from source)
# pip install git+https://github.com/StarNetLaboratory/GenSLM-2.git
